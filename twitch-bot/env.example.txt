# ── Twitch Bot Account ──
TWITCH_BOT_USERNAME=niklaus0x_bot
TWITCH_BOT_PASSWORD=your_bot_account_password
TWITCH_BOT_EMAIL=your_bot_email@gmail.com
TWITCH_BOT_DOB=01/15/1990

# ── Channel to join ──
TWITCH_CHANNEL=niklaus0x

# ── Auto-filled by setup scripts ──
TWITCH_OAUTH_TOKEN=oauth:auto_filled_by_setup
TWITCH_CLIENT_ID=auto_filled_by_setup
TWITCH_CLIENT_SECRET=auto_filled_by_setup

# ── Quick Start ──
# 1. cp env.example.txt .env
# 2. Fill in TWITCH_BOT_USERNAME, TWITCH_BOT_PASSWORD, TWITCH_BOT_EMAIL
# 3. npm install && npx playwright install chromium
# 4. npm run setup   ← automates everything else!
# 5. npm start
